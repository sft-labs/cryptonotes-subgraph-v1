{
  "name": "notes-graph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ crypto-notes/notes-graph",
    "create-local": "graph create --node http://localhost:8020/ crypto-notes/notes-graph",
    "remove-local": "graph remove --node http://localhost:8020/ crypto-notes/notes-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crypto-notes/notes-graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
